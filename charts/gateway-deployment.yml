##write a deployment file for gateway service

apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-deployment
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
      - name: gateway
        image: 317884193661.dkr.ecr.eu-west-1.amazonaws.com/isolates:81a48ff5be9d7d0d1a366df3dbbeadfe9bad8fb7
        ports:
        - containerPort: 8889
        env:
        - name: LOGIN_SERVICE_BASE_URI
          value: "http://login-service:8888"
        resources:
          limits:
            memory: "512Mi"
            cpu: "1000m"
          requests:
            memory: "256Mi"
            cpu: "500m"
